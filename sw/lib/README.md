## ASTRARV32 Software Library

This folder contains the ASTRARV32 hardware abstraction layer (HAL), runtime
environment, and several helper/auxiliary functions. Application programs
should only include the *main ASTRARV32 define file* `astrarv32.h`. This file
automatically includes all other header files.

```c
#include <astrarv32.h>
```
